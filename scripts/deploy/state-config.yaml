aws:
  state:
    bucket_name: agent-kernel-terraform-state-bucket-dev
    region: ap-southeast-2

azure:
  state:
    storage_account: akterraformstate
    container_name: tfstate
    resource_group: ak-tfstate-rg

gcp:
  state:
    bucket_name: agent-kernel-terraform-state-bucket
    project_id: agent-kernel-test
    region: us-east1