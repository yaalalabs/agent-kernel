aws:
  state:
    bucket_name: agent-kernel-terraform-state-bucket-akdev
    region: ap-southeast-2

azure:
  state:
    storage_account: akterraformstate
    container_name: tfstate
    resource_group: ak-tfstate-rg