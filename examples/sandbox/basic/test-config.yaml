# Agent Kernel test harness configuration (loaded only when running tests).
# See https://kernel.yaala.ai/docs/testing/overview for details.
#
# The sandbox executes real code, so every expected answer is an exact value
# ("42", "hello sandbox", "328"). Deterministic exact-match matching keeps the comparison
# offline: no LLM judge is involved in evaluating results.
mode: score
