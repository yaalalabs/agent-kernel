
[project]
name = "cli-crewai"
version = "0.1.0"
description = "Demo of CrewAI based agents running in Agent Kernel with a CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ak[cli,crewai]>=0.1.0a1",
]

[tool.uv]
package = false