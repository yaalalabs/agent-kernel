[project]
name = "cli-google"
version = "0.1.0"
description = "Demo of Google ADK based agents running in Agent Kernel with a CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ak[cli,adk,test]>=0.1.0a1",
]

[project.optional-dependencies]
dev = [
    "ak[test]>=0.1.0a1",
]

[tool.uv]
package = false
