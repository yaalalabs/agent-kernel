[project]
name = "cli-multi"
version = "0.1.0"
description = "Demo of CrewAI and OpenAI Agents SDK based agents running in Agent Kernel with a CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agentkernel[cli,crewai,openai]>=0.1.2b25",
]

[project.optional-dependencies]
dev = [
    "agentkernel[test]>=0.1.2b25",
]

[tool.uv]
package = false