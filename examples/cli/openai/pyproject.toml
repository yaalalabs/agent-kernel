[project]
name = "cli-openai"
version = "0.1.0"
description = "Demo of OpenAI Agents SDK based agents running in Agent Kernel with a CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agentkernel[cli,openai]>=0.1.2b22",
]

[project.optional-dependencies]
dev = [
    "agentkernel[test]>=0.1.2b22",
]

[tool.uv]
package = false
