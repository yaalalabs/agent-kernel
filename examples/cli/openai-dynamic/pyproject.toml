[project]
name = "cli-openai-dynamic"
version = "0.1.0"
description = "Demo of OpenAI Agents SDK based agents running in Agent Kernel with a CLI with the support for dynamic loading of agents"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agentkernel[cli,openai]>=0.2.1",
]

[project.optional-dependencies]
dev = [
    "agentkernel[test]>=0.2.1",
]

[tool.uv]
package = false
