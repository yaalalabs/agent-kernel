[project]
name = "aws-serverless-openai"
version = "0.1.0"
description = "Demo of OpenAI Agents SDK based agents running in Agent Kernel on AWS Lambda"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agentkernel[openai,aws]>=0.2.0b1"
]

[tool.uv]
package = false
