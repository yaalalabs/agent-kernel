[project]
name = "aws-serverless-adk"
version = "0.1.0"
description = "Demo of Google ADK based agents running in Agent Kernel on AWS Lambda"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agentkernel[adk,aws]>=0.1.0a1"
]

[tool.uv]
package = false
