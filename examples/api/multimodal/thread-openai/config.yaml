multimodal:
  enabled: true
  storage_type: in_memory # thread mode needs a shared attachment store (in_memory, redis, or dynamodb)

thread:
  type: memory # other supported backends: redis | valkey | dynamodb | firestore | cosmosdb
