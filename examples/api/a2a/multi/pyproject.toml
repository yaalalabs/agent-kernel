[project]
name = "api-a2a-multi"
version = "0.1.0"
description = "Demo of CrewAI and OpenAI Agents SDK based agents running in Agent Kernel with a API exposed via A2A"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ak[cli,crewai,openai,a2a,test]>=0.1.0a1",
]

[project.optional-dependencies]
dev = [
    "ak[test]>=0.1.0a1",
]

[tool.uv]
package = false