[project]
name = "api-a2a-multi"
version = "0.1.0"
description = "Demo of CrewAI and OpenAI Agents SDK based agents running in Agent Kernel with a API exposed via A2A"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agentkernel[cli,crewai,openai,a2a]>=0.1.2b11",
]

[project.optional-dependencies]
dev = [
    "agentkernel[test]>=0.1.2b11",
]

[tool.uv]
package = false