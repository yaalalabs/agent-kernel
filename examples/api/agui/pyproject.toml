[project]
name = "agui-openai"
version = "0.1.0"
description = "Demo of an Agent Kernel agent driven over the AG-UI protocol, with a React frontend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agentkernel[openai,agui,api]>=0.8.1",
]

[dependency-groups]
dev = [
    "agentkernel[test]>=0.8.1",
    "black==26.5.1",
    "isort>=5.0.0",
    "mypy>=1.0.0",
]

[tool.uv]
package = false

[tool.isort]
profile = "black"
line_length = 120

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true

[tool.black]
line-length = 120
target-version = ["py312"]
