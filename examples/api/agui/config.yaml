agui:
  default_agent: planner # also serves POST /agui
  # agents: [planner]    # omitted = every streaming-capable agent
  state:
    enabled: true # get_agui_state / update_agui_state + StateSnapshot
  client_context:
    enabled: true # get_forwarded_props / get_agui_context

api:
  custom_router_prefix: "" # demo UI at GET / instead of GET /custom/
