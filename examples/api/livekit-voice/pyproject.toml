[project]
name = "livekit-voice-example"
version = "0.1.0"
description = "Agent Kernel LiveKit Realtime Voice Example"
requires-python = ">=3.12"
dependencies = [
    "agentkernel[livekit,openai,api] >=0.9.2"
]
