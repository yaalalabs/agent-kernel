[project]
name = "aws-serverless-crewai"
version = "0.1.0"
description = "Demo of CrewAI based agents running in Agent Kernel on AWS ECS"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agentkernel[crewai,api]>=0.1.0a1"
]

[tool.uv]
package = false
