[project]
name = "aws-containerized-adk"
version = "0.1.0"
description = "Demo of Google ADK based agents running in Agent Kernel on AWS ECS"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agentkernel[adk,api]>=0.1.2b5"
]

[tool.uv]
package = false
