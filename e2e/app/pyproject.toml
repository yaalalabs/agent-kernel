[project]
name = "ak-e2e-messaging-app"
version = "0.1.0"
description = "Agent Kernel end-to-end test deployment: one OpenAI agent exposed via Slack and Telegram on AWS ECS"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agentkernel[api,openai,slack,telegram,gmail,whatsapp,messenger,instagram]>=0.8.1",
    "aiohttp>=3.14.1",
]

[dependency-groups]
dev = [
    "black==26.5.1",
    "isort>=5.0.0",
]

[tool.uv]
package = false

[tool.isort]
profile = "black"
line_length = 120

[tool.black]
line-length = 120
target-version = ["py312"]
